# Changelog

Internal demo project. One line per material change, newest at the top. Phase headings preserved for git/PR cross-reference; for the full "why" check the corresponding commit message or the per-feature doc in `docs/output/`.

## Unreleased

- Added a fifth claim takeover card, `AwbFailedCard` (`claim.awbFailure`): a pre-pickup gate for when the courier can't validate the pickup address so the airway bill can't be generated — danger-tone "We couldn't book a pickup" → confirm/edit address → warn-tone "generating label" (journey-driven via `handleConfirmAddress`/`claim_need_address`, local fallback for the standalone mock 89944). Routed ahead of `PickupFailedCard`. Extracted the shared `EditableContactCard` (address·phone·email read-only⇄edit) from `PickupFailedCard`'s private copy; wired the AWB journey nodes (`claim_awb_generated`/`claim_awb_failed`/`claim_need_address`) into change-of-mind + issue journeys; `ClaimCard`'s scheduled-pickup strip now gates on `scheduledPickup.awb` (shows `ArrangingPickupStrip` placeholder + an AWB row otherwise); new `awb_generation_failed` gate in `actionGateCopy`. `src/components/{AwbFailedCard,EditableContactCard,ClaimCard,PickupFailedCard}.jsx`, `src/App.jsx`, `src/lib/claims.js`, `src/data/orders/claims.js`, `src/data/journeys/claim{ChangeOfMind,Issue}.js`. See `docs/output/returns/claim_tracking.md` §3.5.
- Added a quiet "Verified by NSYS" third-party device condition-report link to the delivered `PastOrderCard`, rendered under the product row via a new `ProductSummary` `afterRow` slot and driven by an optional `order.conditionReport` (`{ url, reportId }`) field (seeded on three baseline delivered orders + the initial-order journey; URL is a decorative placeholder). New `ConditionReportChip` (local to `PastOrderCard.jsx`) + `public/nsys-icon.svg`. `src/components/{PastOrderCard,ProductSummary}.jsx`, `src/data/orders/baseline.js`, `src/data/journeys/initialOrder.js`. See `docs/output/orders.md` §3.3, §7.5.
- Reworked the evidence-step proof attachment into **guided slotted capture** (`IssueEvidence`): the examples card now collapses behind an `Add your proof` CTA that expands into per-item upload slots — one issue slot driven by `sub.mediaType` + the four universal photos (Screen / Back & camera / Accessories / Packed safely), each single-file with a placeholder thumbnail + remove. Files persist across collapse (`issueDetails.proofSlots` map). Proof is now **advisory, not gated** — a live `N of M added` counter + amber "you can continue, but missing proof can delay your claim" warning, never blocking Continue (the old `attachment` gate + `proofOptional` lift removed from `flowReducer.stepError`; description still required). `Step6Review`'s summary now shows a `Proof` list per slot and drops a redundant inner "Issue" label. Applies to issue / warranty / wrong-item; compensation + seeded mocks keep the single-`attachmentName` model untouched. `src/components/ClaimFlow/{IssueEvidence,StepEvidence,Step6Review,flowReducer}.jsx/js`. See `docs/output/returns/issue.md` §2.3–2.3.1.
- Added a mock inline NPS/CSAT survey (`NpsSurvey`) on the returns confirmation screen (Step 7, all claim types): a soft brand-tinted card asking "How was your experience in raising a claim with Revibe?" over a 1–5 Poor→Excellent scale, flipping to a local thank-you state on tap. Stands in for a future Typeform embed (no network); swap-in point marked in the file. `src/components/NpsSurvey.jsx` (new), `src/components/ClaimFlow/Step7Confirmation.jsx`. See `docs/output/returns/change_of_mind.md` §2.9.
- Strengthened the guided-reset charge messaging: the Step 3 `Callout` now reads "Reset the device before we can refund you. If it reaches us still locked, your refund may be delayed and a fee deducted" (was the unlock/resale framing), and the Step 6 device-prep ack subtitles gained an amber `text-warn` caveat that an unreset/locked device may be delayed and charged a fee (factory-reset + never-set-up paths; seeded unlink path left neutral). Copy-only. `src/components/ClaimFlow/{Step3DevicePrep,Step6Review}.jsx`. See `docs/output/returns/guided_reset.md` §4–§5.
- Integrated per-issue **example proofs** across the evidence step with a tiered, graceful-degradation model in `IssueEvidence` + `issueTaxonomy.js`: an issue shows its own bespoke `examples` if it has them (`exampleKind: 'specific'`, heading `Approved examples`), else its **category `goldenExample`** fallback (`exampleKind: 'golden'`, heading `Example of good proof`), else just the universal checklist — resolved in `evidenceSubFor`/`categoryForIssue` (new export). Added a category golden per `ISSUE_CATEGORIES` entry (keyed to the verification hint — up-close damage / on-screen error / lock prompt / fault video) and bespoke sets for `prev_owner_pw` + `overheat`. Added a per-issue `coverage` ("When we cover this", success-toned) row and surfaced the previously-dead `tryFirst` ("Try this first", brand-tinted) row in Group A. Added a reusable `proofOptional` flag — `overheat` lifts the `attachment` gate in `flowReducer.stepError` (description still required), swaps the uploader's "Required…" warn for a muted "Optional for this issue…", and tags its example strip with an `Optional` chip. Collapsed media chips to **two types only — `Photo` / `Video`** (`MEDIA_CHIPS`): `screenshot`→Photo, `voice`→Video, and the "Something else" catch-alls (`mediaType: 'both'`) show both; `mic` copy drops "voice memo" and `linked_account` copy drops the (impossible) "screenshot" of a locked screen → "photo". Demoted the `How to provide valid proof` link to a quiet secondary (inline-first, no redirect). New proof assets under `public/proof/{golden,account-lock,overheating}/`. `src/components/ClaimFlow/{IssueEvidence.jsx,issueTaxonomy.js,flowReducer.js}`, `public/proof/*`. See `docs/output/returns/issue.md` §2.2–2.3.1, §8.
- Rebuilt the raise-a-claim decision phase from outcome-first to **situation-first**: new Screen 1 (`Step1Situation` — `changed_mind` / `device_fault` / `wrong_item` / `keep_compensation`) → per-branch screens (`StepIssueCategory` → `StepIssueSpecific` for faults; `StepWrongItem` for wrong items; `StepRemedy` resolves refund/repair/replacement) → an extracted `StepEvidence` proof step, with `claimType` *derived* from `situation`+`remedy` (`claimTypeFor`) rather than picked. `flowReducer` rewritten (`DECISION_STEPS`+`tailSteps`→`sequenceFor`, `progressFor` macro steps), `ProgressBar` now collapses the decision phase to one "Step 1" with sub-fill, and a new `issueTaxonomy.js` (6 categories ≤5 issues + 4 wrong-item details, brand-conditional S Pen / iCloud-Google copy) replaces the deleted `issueSubtypes.js`. `Step2Reason` reframed to 6 no-fault reasons + 2 tripwires, `SwitchFlowSheet` simplified to a single-target benefit sheet, `Step2Compensation` to 3 problems (`accessory_missing`/`accessory_broken`/`shipping_refund`) + agent-review banner. Replacement (`wrong_item`→correct item) skips the refund step with its own Review/Confirmation copy + stock disclaimer. Deleted `Step1ClaimType`/`Step2IssueDetails`/`issueSubtypes.js`; migrated seeded mocks (`battery`→`battery_drain`, `charger`→`accessory_broken`); `lib/claims.js` `REASON_LABELS` re-keyed with a legacy `changed_mind` alias. `src/components/ClaimFlow/{Step1Situation,StepIssueCategory,StepIssueSpecific,StepWrongItem,StepRemedy,StepEvidence,BatteryHealthCheck,issueTaxonomy}.jsx/js` (new), `{ClaimFlow,flowReducer,ProgressBar,Step2Reason,Step2Compensation,SwitchFlowSheet,Step6Review,Step7Confirmation,compensationSubtypes}`, `src/lib/claims.js`, `src/data/orders/compensation.js`, `src/data/journeys/claim{Issue,Warranty,Compensation}.js`. See `docs/output/returns/change_of_mind.md` §0/§2.2–2.3.2 + `issue.md` §2.2–2.3.
- Redesigned the issue-flow Step 2 (`Step2IssueDetails`) evidence section into a reusable `IssueEvidence` proof card: one white card with two labelled groups — `For this issue` (the sub-type's `need` ask + a media-type chip + captioned approved-example thumbnails) and a universal `For every return` 4-photo checklist (screen · back & camera · accessories · packed safely) — above the single gated uploader, with every thumbnail tappable into a paging `ProofLightbox`. Added `mediaType` to every sub-type and `examples` to `battery` in `issueSubtypes.js`; the standalone warn banner folded into the empty drop-zone, the uploader moved above the description (so `stepError` order is now `subtype → attachment → description`), and proof images live under `public/proof/`. `src/components/ClaimFlow/IssueEvidence.jsx` (new), `Step2IssueDetails.jsx`, `issueSubtypes.js`, `flowReducer.js`, `public/proof/*`. See `docs/output/returns/issue.md` §2.3.
- Added a `StatusExplainer` inline "ⓘ Learn more" affordance beside the status chip on the four cards whose header otherwise doesn't define the stage (`InProgressCard`, `ClaimCard` when no `actionRequired`, `WarrantyClaimCard`, cancelled `PastOrderCard`); tapping reveals a full-width plain-language stage definition below the chip row (stops propagation so the card doesn't toggle). Copy is data-driven from the source-of-truth libs: `STATUS_EXPLANATIONS` + `statusExplanation(order)` (`created` / `quality_check` + `cancellation_*` — the only order stages that route through the explainer) in `lib/statuses.js`, and `CLAIM_EXPLANATIONS` / `COMPENSATION_EXPLANATIONS` / `WARRANTY_EXPLANATIONS` + `claimExplanation` / `warrantyClaimExplanation` in `lib/claims.js`. Self-explaining heroes (delivered/`HeroCard`/takeovers) and shipped/delivered `OrderCard` (already bannered) are intentionally left out. `src/components/StatusExplainer.jsx` (new), `src/lib/{statuses,claims}.js`, `src/components/{InProgressCard,ClaimCard,WarrantyClaimCard,PastOrderCard}.jsx`. See `docs/output/orders.md`, `docs/output/returns/claim_tracking.md`, `docs/output/warranties_compensations.md`.

## 2026-06-19

- Fixed the `OrderClaimLink` compact-row left accent strip overrunning the card's rounded corners by clipping it with `overflow-hidden` (matching `PreCancellationOrderCard`). `src/components/OrderClaimLink.jsx`.
- Retired `OriginalOrderSheet`: deleted the component and removed its `View order details` trigger from `OrderClaimLink`, so the expanded real `PastOrderCard` (or `PreCancellationOrderCard`) is now the only order surface in the order↔claim accordion — order total / payment are covered by `ProductSummary`'s breakdown and the compact-row taps own the reciprocal flip. `src/components/OrderClaimLink.jsx` (deleted `src/components/OriginalOrderSheet.jsx`). See `docs/output/returns/claim_tracking.md` §10.
- Reworked `OrderClaimLink` from a parent-order strip into a one-open accordion pairing the real order half (delivered `PastOrderCard`, or a neutral `PreCancellationOrderCard` for a cancelled order) above the claim/cancellation half, joined by a measured connector rail; the closed half collapses to a `View ▸` compact row, `OriginalOrderSheet` is kept behind a `View order details` trigger, and the prop signature is now `{ order, defaultOpen, children }` (the `onReveal`/ring-pulse round-trip retired). `src/components/OrderClaimLink.jsx`. See `docs/output/returns/claim_tracking.md` §10.
- Promoted and relabelled the `Raise a claim` CTA on the delivered `PastOrderCard` and the in-flight `HeroCard`: it now reads `I need help with this device` and renders as a prominent full-width button (brand-tinted on `PastOrderCard`, translucent-white on `HeroCard`), with `Download receipt` demoted to a quiet secondary control. `PastButton` gained a `tone`/`full` API. `src/components/{PastOrderCard,HeroCard}.jsx`. See `docs/output/orders.md` §3.3–3.4.
- Refined the split-refund display: when `RefundSplitRows` shows it now takes the "Going to / Sent to" wording via a `caption` prop and replaces the single destination-chip summary, and for a BNPL split-paid order the `BnplDisclaimerTooltip` rides on the card-portion row (new internal `labelAfter`) while the standalone tooltip is suppressed on every split surface (no double tooltip). `src/components/RefundSplitRows.jsx`, `src/components/{ClaimCard,ClaimDetailsSheet}.jsx`, `src/components/ClaimFlow/Step{5RefundMethod,6Review,7Confirmation}.jsx`. See `docs/output/orders.md` §7.1.
- Added proportional card+gift-card refund splits: a new optional `order.paymentSplit = { card, giftCard }` marks an order split-paid, and on the **original-payment** refund path the refund is paid back along the same ratio — card portion → card, gift-card (store-credit) portion → Wallet — with any deduction kept proportional. New `isSplitPaid`/`refundDestinations` (`lib/returns.js`) + a shared `RefundSplitRows.jsx` render the two-destination rows across the cancel sheet, returns Steps 5/6/7, refund/claim detail sheets, and the refund-hero/claim cards; `lib/wallet.js` credits the gift-card portion to the ledger (non-switchable). Seeded mocks (`#89712`/`#89702`/`#89518`/`#89868` + a wallet seed tx) and the `cancellation` + `claim_change_of_mind` journeys' `initialOrder` (split on the existing card/original fork; wallet fork unchanged; `INITIAL_ORDER` untouched). `src/lib/{returns,wallet}.js`, `src/components/RefundSplitRows.jsx` (new), `src/components/{CancelOrderSheet,ClaimCard,ClaimDetailsSheet,RefundDetailsSheet,PastOrderCard}.jsx`, `src/components/ClaimFlow/Step{5RefundMethod,6Review,7Confirmation}.jsx`, `src/data/orders/{baseline,claims}.js`, `src/data/{wallet,journey}.js`. See `docs/output/orders.md` §7.1, `cancellations.md`, `wallet.md`, `returns/{change_of_mind,issue,claim_tracking}.md`, `journey_backend_spec.md`.

## 2026-06-18

- Added a raise-a-claim-before-delivery branch to all four claim journeys: the in-flight `HeroCard`'s `Raise a claim` is now live (`onRaiseClaim`), and a shared `withInTransitClaim` decorator (`src/data/journeys/inTransitClaim.js`) grafts a silent agent delivery-confirmation step onto every in-transit shipment stage — submit advances `claim_raised_in_transit__<stage>`, then forks to `claim_delivery_confirmed__<submit>` (flips the order to `delivered` + seeds the claim at `initiated`, reusing the journey's submit pipeline) or `claim_delivery_unconfirmed__<stage>` (back to the same stage, no claim). `App.handleSubmitClaim` routes the pre-delivery raise; projected-order memo flips a hero-raised submitted claim to delivered. New `claim.delivery_check.*` events are unauthored ⇒ silent. `src/data/journeys/inTransitClaim.js` (new), `src/data/journey.js`, `src/components/HeroCard.jsx`, `src/App.jsx`. See `docs/output/journey_backend_spec.md`.
- Added a terminal `ClosedClaimCard` for claims Revibe rejected at review (`claim.closure` → `isClaimClosed`): muted "Claim closed" / "No refund issued" chrome with a reason-driven hero (`CLAIM_CLOSURE_REASONS` / `closureCopyFor` in `lib/claims.js` — `outside_window` / `not_eligible` / `evidence_insufficient` / `not_reproduced` / `duplicate` / `other`), an attributed Revibe-Quality ops note, and a `Raise a new claim` path; lives in Past orders (`hasActiveClaim` short-circuits on `closure`), routed below `isClaimRefunded`. Hand-seeded mock 89705. `src/components/ClosedClaimCard.jsx`, `src/lib/claims.js`, `src/App.jsx`, `src/data/orders/claims.js`. See `docs/output/returns/claim_tracking.md` §3.6.
- Extended the order↔claim link to cancelled orders: `CancelledOrderCard` now wraps in `OrderClaimLink` (parent order strip + connector), its top eyebrow reads `#{cancellationRef}` while the hero eyebrow drops to plain `Cancellation`, and `OriginalOrderSheet` renders a reciprocal `Linked cancellation` row (refund-phase tone) when an order has no claim. `src/components/{PastOrderCard,OriginalOrderSheet}.jsx`. See `docs/output/cancellations.md` §3 + `docs/output/returns/claim_tracking.md` §10.

## 2026-06-17

- Added a shared order↔claim link across the whole claim-card family: new `OrderClaimLink` wrapper (parent order strip + connector thread + post-round-trip `ringPulse` highlight) and `OriginalOrderSheet` (original order + reciprocal "Linked claim" row) wrap `ClaimCard` / `WarrantyClaimCard` / the four takeover cards. Claim refs are now typed and formatted at display time via `formatClaimRef` (`RET`/`WAR`/`CMP`/`CXL` by `claim.type`); `generateClaimRef` returns the bare ref only, and every card eyebrow / hero / sheet drops the `Order · #{id}` eyebrow in favour of the ref + parent strip. Journey `INITIAL_ORDER.id` → `89610`. `src/components/{OrderClaimLink,OriginalOrderSheet,ClaimCard,WarrantyClaimCard,DocsRejectedCard,PickupFailedCard,ResetFailedCard,InvalidClaimCard,ClaimDetailsSheet}.jsx`, `src/lib/{claims,returns}.js`, `src/data/journeys/initialOrder.js`, `tailwind.config.js`. See `docs/output/returns/claim_tracking.md` §10.

## 2026-06-12

- Harmonized every claim courier-leg `See detailed tracking` dropdown into one shared `TrackingDropdown` (`ReturnShipmentTracking.jsx`): `ClaimCard`'s inbound and `WarrantyClaimCard`'s inbound now reuse it (dropping the per-card `ClaimTransitDetail` / `PickupTransitDetail` helpers); `ReturnShipmentTracking` becomes a thin return-leg adapter. The copyable courier/AWB strip is replaced by a Track package (→ DHL test shipment) / Get Help (placeholder) action row; `CourierStrip` export removed. `src/components/{ReturnShipmentTracking,ClaimCard,WarrantyClaimCard}.jsx`.
- Reworded the change-of-mind Step 1 claim-type label from "I changed my mind" to "My device works great, but I want to return it". `src/components/ClaimFlow/Step1ClaimType.jsx`.

## 2026-06-11

- **Added a "Reset guide" previewer to `JourneyDevPanel` (journey mode only).** A new `src/components/ResetGuidePicker.jsx` chip row — iPhone · MacBook · Samsung · Tablet · Laptop — opens `ResetGuideSheet` at its intro for the resolved variant, running the **real** `lib/devices.js` `deviceTypeForCategory` mapping (the same Step 3 seeds from `order.category_name`), so the five guide variants are previewable without hunting for an order of the right category and walking the claim flow. The OS-ambiguous **Tablet** reveals an Apple/Android sub-chooser (→ `ipad` / `android_tablet`), mirroring Step 3's `TabletPicker`; **Laptop** (Windows, no guide yet) falls back to the iPhone guide — both surfaced in a muted footnote. Self-contained (owns its open/tablet state, `ResetGuideSheet` portals itself), rendered after `CountryPicker`; no `App.jsx` or prop changes. `src/components/{ResetGuidePicker,JourneyDevPanel}.jsx`. See `docs/output/returns/guided_reset.md`.
- **Polished the Android remote-reset step-1 mock (`GoogleRemoveRemote`).** Reworked the Google "Your devices" screen from a device-list-card + a disconnected "Manage / Sign out" card into the device-**detail page** Google actually lands you on after tapping in: back-breadcrumb + account avatar, a centered device coin/name/`Galaxy · Android 14` line (`whitespace-nowrap`, fixing the orphaned "ago" wrap), a session-details card (Account · First signed in · Recent activity · Browser), and a single brand-highlighted **Sign out** button — which also fills the dead space in the lower frame. Reconciled the matching step `lead` copy (no more "⋮ menu") on both the phone and tablet remote routes; the `android_tablet` variant inherits the new look via the shared component. Dropped the now-unused `MoreVertical` import. `src/components/ClaimFlow/resetGuideMocks.jsx`, `src/components/ClaimFlow/ResetGuideSheet.jsx`. See `docs/output/returns/guided_reset.md` §2.
- **`InvalidClaimCard`'s `Pay` / `Decline` / `I changed my mind and will pay` CTAs now drive the journey (dev panel moves in lockstep), and the card's state is derived from the order data instead of a local `mode` + sync effect.** Previously the buttons only toggled local card state while the dev panel stayed at the `claim_invalid_confirmed` step, and the reverse-after-decline silently desynced (the panel advanced but the card stayed on the closed state, because the order ended up with both `declinedAt` and `paidAt` and the forward-only effect only flipped `paid` *from* `action_needed`). Now: `Pay` and the reverse both call a `validNext`-gated `handlePayReturnShipping` (`App.jsx`) → `claim_return_shipping_paid`; `Decline` calls `handleDeclineReturn` → `claim_invalid_declined`. `claim_invalid_declined` is **no longer terminal** — it forks to `claim_return_shipping_paid`, so the reverse advances forward in lockstep (added in all three journeys: `claimChangeOfMind`, `claimIssue`, `claimWarranty`). `InvalidClaimCard` now derives `mode` from `invalidClaim.paidAt`/`declinedAt` (**paidAt wins**, so the lingering `declinedAt` after a reversal still reads as paid) with a `localMode` override for the standalone mock; the forward-only sync effect is gone, so a dev-panel **Back** re-syncs the card both ways. No notification copy touched (`claim.ship_back.created` / `claim.declined` already authored). `src/components/InvalidClaimCard.jsx`, `src/App.jsx`, `src/data/journeys/{claimChangeOfMind,claimIssue,claimWarranty}.js`. See `docs/output/returns/claim_tracking.md` §3.3, `journey_backend_spec.md`.
- **`Submit details` on the reset-failed takeover now drives the journey (dev panel moves in lockstep), and the unlock-details submission is split from QC resume by a new `24h passed — reset done` step.** Previously the button only flipped local card state while the dev panel stayed at `via UI · Next: Unlock details received`, and `claim_reset_details_received` cleared `resetFailed` and jumped straight to the QC outcome. Now: the button calls a new `validNext`-gated `handleSubmitResetDetails` (`App.jsx`) that advances `claim_reset_details_received` (or `claim_reset_retry_resubmitted` after a retry); `ResetFailedCard` reads `claim.resetFailed.submittedAt` live (local fallback for the standalone mock, masked passcode → `••••42`) so the *Details received* state and the dev panel stay in sync through Back/Next. The submission node now keeps `resetFailed` (adds `submittedAt`) instead of advancing, and a new `system` `claim_reset_complete` node ("24h passed — reset done") clears `resetFailed`, resumes QC (`ClaimCard` "Under Quality Check"), and forks to the QC outcome — shared by both the first submission and the retry resubmission. Applied to all three journeys with the branch (`claimChangeOfMind`, `claimIssue`, `claimWarranty`). New event `claim.reset.completed` is unauthored ⇒ silent (no notification copy touched). `src/components/ResetFailedCard.jsx`, `src/App.jsx`, `src/data/journeys/{claimChangeOfMind,claimIssue,claimWarranty}.js`. See `docs/output/returns/claim_tracking.md` §3.4, `journey_backend_spec.md`.
- **`Confirm new pickup` now drives the journey (dev panel moves in lockstep), and pickup-failed reschedule is split into a distinct reschedule → picked-up sequence.** Previously the button only flipped local card state while the dev panel stayed stuck at `via UI · Next: Pickup rescheduled`, and the `claim_pickup_rescheduled` node bundled reschedule + collection together (jumping straight to the Pickup stage). Now: the button calls a new `validNext`-gated `handleConfirmReschedule` (`App.jsx`) that advances `claim_pickup_rescheduled`; `PickupFailedCard` reads `claim.pickupFailure.rescheduledAt` live (with a local fallback for the standalone mock) so the card's "new pickup on the way" state and the dev panel stay in sync through Back/Next. `claim_pickup_rescheduled` is now reschedule-only (sets `rescheduledAt`, keeps the failure) and re-merges at `claim_picked_up`, which gained `pickupFailure`/`actionRequired` clears and now carries the inbound country fork for both the happy and reschedule paths (AE/ZA → detailed transit, SA/Others → QC). Applied to all three journeys with the branch (`claimChangeOfMind`, `claimIssue`, `claimWarranty`). No notification copy touched (`claim.transit.picked_up` already authored). `src/components/PickupFailedCard.jsx`, `src/App.jsx`, `src/data/journeys/{claimChangeOfMind,claimIssue,claimWarranty}.js`. See `docs/output/journey_backend_spec.md`, `returns/claim_tracking.md` §3.2, `country_split.md` §6.
- **Removed the in-card `Undo · replay the demo` buttons from the four claim takeover cards.** They predated journey mode and only reset local component state; replaying a post-commit takeover state is now done via `JourneyDevPanel` Back/Next, which walks the same takeover nodes (`docsRejection` / `pickupFailure` / `resetFailed` / `invalidClaim` incl. `paidAt`/`declinedAt`). Dropped the five buttons (`ResetFailedCard`, `PickupFailedCard`, `DocsRejectedCard` — the latter's `Undo · edit before review starts` — and both `InvalidClaimCard` branches), their `onUndo` props/handlers and "Demo only" comments, and the now-unused `RotateCcw` import from the three cards that no longer reference it (`InvalidClaimCard` keeps it). Forward action CTAs (`Confirm new pickup`, `Submit details`, `Pay …`, `Decline`, `I changed my mind …`) are unchanged. `src/components/{ResetFailedCard,PickupFailedCard,DocsRejectedCard,InvalidClaimCard}.jsx`. See `docs/output/returns/claim_tracking.md` §3.
- **Fault reason caught in change of mind now lets the customer choose refund vs replacement.** Previously a `defective` / `wrong_item` / `damaged` reason picked inside the change-of-mind flow auto-routed to warranty (if the order carried the paid Revibe Care add-on, `order.warranty > 0`) or issue otherwise. Since every device carries at least a 1-year warranty, a **replacement is always available** — so `routeForReason` stops gating on `order.warranty` (returns the recommended default `warranty`) and a new `isRemedyChoice(reasonId, claimType)` (`Step2Reason.jsx`) flags the fork. `SwitchFlowSheet` gained a **choice mode** (`choice` + `onChoose` props): a "This sounds like a fault" header + `ProductSummary` + two tappable cards — **Get a replacement** (warranty, brand-toned `Recommended` badge) and **Get a refund** (issue) — each dispatching `ROUTE_FROM_REASON` with its own target; single "Choose another reason" footer. All popup icons are brand-toned. Step 1's faulty-device option relabelled `Return for a refund or replacement` → `Return for a refund`. `src/components/ClaimFlow/{Step2Reason,SwitchFlowSheet,ClaimFlow,Step1ClaimType}.jsx`, `issueSubtypes.js`. See `docs/output/returns/change_of_mind.md` §2.3 + §5.
- **Removed the date-range filter; the search bar now spans the full width.** `OrderFilters` dropped the `Last 30 days / Last 3 months / Last year / All time` dropdown (and its `DATE_RANGES` export, `activeRange`/`onRangeChange` props, and open-state plumbing) — it was visibly inert because every mock order fell inside every range. The "Find an order or item" search field now takes the whole row. `App.jsx` lost the now-dead `RANGE_DAYS`, `parsePlacedAt`, `matchesRange`, the `activeRange` state, and the `dateFiltered` memo (counts/filter now derive directly off `projectedOrders`). `src/components/OrderFilters.jsx`, `src/App.jsx`. See `docs/output/orders.md` §5.1.
- **Revibe Wallet brought to life — tappable pill / card chip → balance + transactions sheet, with "Move to card" on the latest store-credit refund.** The `GreetRow` wallet pill (was a decorative hardcoded `AED 384`) and the "Revibe Wallet" refund-destination chip on the claim/cancellation cards (`DestinationChip` in `ClaimCard` + the exported one in `PastOrderCard`, also used by `RevibeCancellationCard`) now open a new `WalletSheet` bottom sheet (`onOpenWallet` → `openWallet` in `App.jsx`). Balance is **derived**: new `src/lib/wallet.js` (`walletLedger`/`walletBalance`/`latestSwitchableCredit`/`cardEquivalentFor`) builds the ledger from a curated, used-wallet seed history (`src/data/wallet.js` — refund credits, a referral, purchase debits, ~AED 80 baseline) plus the **live in-context refund**: `App.jsx` passes `journeyMode ? projectedOrders : []`, so replaying the cancellation / change-of-mind journey to its wallet-refunded node surfaces that refund in the ledger. Live refunds **pin to the top** (tiered sort: live `via` claim/cancellation above seed) so the journey refund is always row 1 regardless of its node's hardcoded date. The **single latest refund** is movable (funds-gated, no cascade): **Move to card** opens a confirm view re-applying the deduction store credit had waived via `cardEquivalentFor` — 10% restocking (change-of-mind), AED 100 bonus forfeited (issue / breached cancellation), 5% processing (normal customer cancellation), and zero deduction / full amount for Revibe-initiated cancellations + compensation. Self-contained + in-session (`walletTransfers`, undoable via `UndoSnackbar` `kind: 'wallet_transfer'`; balance drops by the full credit, row reads "Moved to card"; journey node change clears the order's transfer keys). Lifted the cancellation fee to a shared `CANCELLATION_FEE_RATE` in `src/lib/returns.js` (now also used by `CancelOrderSheet`). `src/lib/wallet.js` (new), `src/data/wallet.js` (new), `src/components/WalletSheet.jsx` (new), `src/components/{GreetRow,ClaimCard,PastOrderCard,RevibeCancellationCard,CancelOrderSheet}.jsx`, `src/App.jsx`, `src/lib/returns.js`. See `docs/output/wallet.md`.
- **Notification variants generalized — any order field can select the copy (`variantBy`).** `notificationFor` now resolves a variant via a new `variantKey(raw, order)` helper: an entry may declare `variantBy: '<order field>'` and `notificationFor` picks `variants[order[variantBy]]`; absent `variantBy`, the axis defaults to the existing claim proof split (`claimRequiresProof` → `proof`/`no_proof`), so `claim.created` is unchanged. A missed lookup resolves to silent (no copy), not an error. First use: `order.cancellation.revibe_initiated` carries `variantBy: 'cancellationReason'` with one apology comm per reason (`item_unavailable` / `price_error` / `undeliverable_address`), each its own `we-are-sorry-*.png` (copy owner-authored). `src/lib/notifications.js`, `src/data/notifications/orders.js`. See `docs/output/journey_backend_spec.md`.
- **Replay panel stack no longer clips off the top.** The bottom-anchored journey overlay (`JourneyNotificationPanel` + `JourneyDevPanel`) gained `max-h-[calc(100vh-2rem)] overflow-y-auto`, so when the stack is taller than the viewport the whole stack scrolls instead of growing past the top edge. `src/App.jsx`.

## 2026-06-10

- **Unified every timeline in the app into one `Timeline` component (new design: hollow pulsing current step + ¾ "in-transit" connector).** A single `src/components/Timeline.jsx` (`orientation` horizontal/vertical, four tones, `onDark`/`dense`/`complete`/`frozen`/`toneForStep`) now renders **every** step/milestone strip. Completed = filled dot + check; current = hollow toned ring with a 3px↔7px breathing glow ring + a 75% pulsing outgoing connector; future = grey hollow; the final step (terminal) renders completed (filled check, no pulse). Two net-new keyframes `timelinePulse`/`timelineConnPulse` in `tailwind.config.js`, both `motion-safe:`-gated (static 4px ring fallback under `prefers-reduced-motion`). Replaced and **deleted** the eleven hand-rolled renderers it absorbed — the spec's five (`StatusTimeline`, `ClaimProgressDots`, `ShippingSubTimeline`, `CancellationSubTimeline`, `ReturnShipmentTracking`'s `SubStatusItem`) plus six more found in the sweep (`OrderCard` `DotBar`, `HeroCard` `DotTimeline`+`SubItem` on the dark gradient via `onDark`, `InProgressCard` `TimelineDots`, `PastOrderCard` `RefundProgressDots`, `ClaimCard` `TransitSubItem`). Behaviour preserved: cancellation keeps its danger-chain → success-`refunded` terminal via `toneForStep`; the cancelled-order collapsed strip freezes at the cancel point (`frozen`, no ✕); past/refunded and credited/delivered strips show all-checks (terminal rule). The horizontal connector spans dot-edge-to-edge so the hollow current ring never shows the line through its centre. `src/components/Timeline.jsx` (new) + `{OrderCard,HeroCard,InProgressCard,PastOrderCard,ClaimCard,WarrantyClaimCard,InvalidClaimCard,ReturnShipmentTracking}.jsx`, `tailwind.config.js`. Design: `docs/handoff/timeline/design.md`. See `docs/output/orders.md` + `returns/claim_tracking.md`.
- **Country split — shipment legs collapse to a single step for SA & Others (first real use of the §6 per-edge country tags).** `detailedTracking` now governs not just the card dropdowns but the **journey sequence + banner copy** for the no-detailed-tracking markets, so the granular logistics milestones (and their destination-country / customs wording) never surface. **Banner:** `descriptionKey(order)` in `src/lib/statuses.js` collapses any `shipped:<subStatus>` to the single `shipped` message when `!countryConfig(order).detailedTracking` (belt-and-suspenders for data-driven orders); the base `shipped` body was reworded to the country-neutral "Your order has shipped and is on its way to you." **Journey forks** (all via country-tagged `next` edges, no engine change): *outbound* — a new `shipped_simple` node (label "Shipped", event `shipment.shipped`, `subStatusId: null`) replaces the four `arrived_destination → cleared_customs → forwarded_to_agent → out_for_delivery` steps for `SA`/`Others`, forked at `qc_started` (and at `cancellation_declined` / `cancellation_kept` in the cancellation journey) across all six journeys; *return leg* (`claim_return_shipping_paid`) and *warranty ship-back* (`claim_ship_back_created`) edge-skip their granular sub-statuses straight to `claim_invalid_return_delivered` / `claim_device_returned`; *inbound pickup* (`claim_picked_up` + the rescheduled re-entry `claim_pickup_rescheduled`) edge-skips the three transit sub-statuses straight to `claim_qc_started`. `AE`/`ZA` walk every granular chain unchanged; the skipped nodes stay in each array, just unreached. `shipment.shipped` has no notification entry yet, so it resolves to `silent` until the owner authors copy (notification content is owner-only — see `CLAUDE.md`). `src/lib/statuses.js`, `src/data/journeys/{happyPath,cancellation,claimChangeOfMind,claimIssue,claimWarranty,claimCompensation}.js`. See `docs/output/country_split.md` §6.
- **Country split — foundation + first difference (no detailed tracking for SA & Others).** New `src/lib/countries.js` is the single source of truth: per-country capability flags keyed by `order.country` (`AE` / `ZA` / `SA` / `Others`, default `AE`) — `{ detailedTracking, expertReview, eddMarket }` — plus `countryConfig(codeOrOrder)` (safe default) / `COUNTRY_CODES` / `DEFAULT_COUNTRY`. Country is an **orthogonal dimension**, not a journey/component fork: a new `src/components/CountryPicker.jsx` chip row (mounted in both `JourneyDevPanel` + `EddSandboxPanel`) and a `?country=<code>` URL param select it, `App.jsx` injects it onto the replayed order (`activeOrderFromJourney`, spread last so it wins). First shipped difference: `countryConfig(order).detailedTracking` gates **every granular-milestone surface** — `HeroCard` (order delivery dropdown), `OrderCard` (expanded "Shipping progress" sub-timeline), `ClaimCard` + `WarrantyClaimCard` (inbound pickup), and the shared `ReturnShipmentTracking` return leg (`WarrantyClaimCard` ship-back + `InvalidClaimCard` paid) — hidden for `SA`/`Others`, shown for `AE`/`ZA`; the top-level courier "Track" card stays, and `ReturnShipmentTracking` stays country-agnostic (gated at its call sites). `src/lib/countries.js`, `src/components/{CountryPicker,HeroCard,OrderCard,ClaimCard,WarrantyClaimCard,InvalidClaimCard,JourneyDevPanel,EddSandboxPanel}.jsx`, `src/App.jsx`. See `docs/output/country_split.md`.
- **Journey engine: per-edge country tags on `next`.** `useJourney(journeyId, country)` now filters each node's `next` edges by the active country — an edge is either a plain id (all countries) or `{ id, countries: [...] }` (only when the active country is listed); untagged edges always pass, so every existing journey is unchanged. This is the prescribed mechanism for journey-flow country differences (e.g. AE's planned expert-revision step before a claim is invalid) without forking a per-country journey. Wired now; no journey uses the tags yet. `src/lib/journey.js`, `src/App.jsx`. See `docs/output/country_split.md` §6.
- **Revibe-initiated cancellation wired into journey mode.** The `RevibeCancellationCard` (previously static-only) can now be replayed via the Cancellation journey. Three terminal nodes — `revibe_cancel_unavailable` / `revibe_cancel_price` / `revibe_cancel_address` (reasons `item_unavailable` / `price_error` / `undeliverable_address`) — are wired into both `placed.next` (created stage) and `qc_started.next` (quality_check stage), the two stages an order can be cancelled by Revibe. They're stage-agnostic (`apply` sets only the cancellation fields — `state`, `cancellationInitiator: 'revibe'`, `cancellationReason`, `cancellationStatusId: 'refunded'`, `cancellationRef`, `reBuyOffer`, a full no-fee `refund`, `cancellationTimeline.refunded` — never `statusId`/`timeline`, so one node per reason serves both entry points), terminal, and always `refunded` (`price_error` → Wallet refund, others → card). `JourneyDevPanel` surfaces them through a grouped **"Cancelled by Revibe"** button (new `RevibeCancelGroup`, keyed on each node's `revibe: { reason, label }` tag) that expands the three reason options instead of cluttering the Next list. No `App.jsx` routing changes (the existing `cancellationInitiator === 'revibe'` Past-orders branch handles it). Event `order.cancellation.revibe_initiated` is silent (no comm copy). `src/data/journeys/cancellation.js`, `src/components/JourneyDevPanel.jsx`. See `docs/output/journey_backend_spec.md` + `cancellations.md` §11.4.
- **Revibe-initiated cancellation card.** New `RevibeCancellationCard` for orders Revibe had to cancel (customer never asked): apology block (reason-specific icon + copy for `item_unavailable` / `price_error` / `undeliverable_address`) → subtle-magenta fixed-amount re-buy offer (copyable code + expiry) → white·magenta "Browse similar devices" CTA (glow/shine/arrow motion, `motion-reduce` safe) → reused `ProductSummary` → collapsible green "fully refunded · no fee" strip (reused `DestinationChip` + `RefundDetailsSheet`). Discriminated by new `cancellationInitiator: 'revibe'`; routes in `App.jsx`'s Past-orders cancelled branch instead of `CancelledOrderCard`. Three static mocks (`89640` / `89641` / `89642`). `DestinationChip` now exported from `PastOrderCard`; CTA keyframes added to `tailwind.config.js`. See `docs/output/cancellations.md` §11 + `docs/handoff/revibe-cancellation/design.md`.
- **Product thumbnail enlarged to a 98×98 fill-zoomed media well (Direction C §6 follow-up).** `ProductSummary`'s 72×72 `object-contain` tile is now a 98px `overflow-hidden` tile whose image fill-zooms to kill the dead white margin. A `CUTOUT_ART` boolean flips two image realities from one code path: transparent cut-out art (now shipped) floats the device on a `brand-bg`→`brand-bg2` radial tint well (faint `white/.10` wash on hero) at `scale(1.04)`; the legacy white-bg placeholder path keeps the clean `surface` + `line` tile (`white/.96` on hero) at `scale(1.3)`. Product images repointed from `/iphone-midnight.png` to the transparent `/iphone-cutout.png` across all order/journey data (`iphone-midnight.png` retained as the placeholder fallback). Thumbnail-only — the Revibe Care callout, Total row, no-warranty collapse, and `tone="light" | "hero"` split are unchanged. `src/components/ProductSummary.jsx`, `public/iphone-cutout.png`, `src/data/**`. See `docs/output/orders.md` §3.0.
- **The hero "Delivering to / Delivered to" line now shows the real address, expandable.** The static `[🏠 Home]` chip is replaced by a new shared `src/components/DeliveryAddressPill.jsx` (props `{ label, address }`) rendering the real `order.address`: a `MapPin` pill showing the first comma-separated segment (e.g. `Ontario Tower`) + chevron, with the full address revealing on a muted line below on tap (label/pill stay pinned on one line, no reflow; chevron/reveal suppressed when there's no second segment). Wired into all four destination heroes — `InProgressCard`, `PastOrderCard`, `WarrantyClaimCard` ship-back, `InvalidClaimCard` paid return — each dropping its inline `Home` import. `src/components/{DeliveryAddressPill,InProgressCard,PastOrderCard,WarrantyClaimCard,InvalidClaimCard}.jsx`. See `docs/output/orders.md` §3.0.1.

## 2026-06-09

- **Aligned the return-shipment milestone dots across the warranty and invalid/return cards.** The `InvalidClaimCard` paid/return surface no longer shows a fresh 4-step *order* timeline (`Created · QC · Shipped · Delivered`) that discarded the claim's history — it now keeps the claim's own head and reads `Initiated · Pickup · QC · Shipped · Delivered`, swapping the refund tail (`refund_issued`/`refund_credited`) for the return shipment's `shipped`/`delivered` (new `RETURN_CLAIM_STATUSES` + `returnClaimProgressIndex(returnShipment)` in `lib/claims.js`; head stamps from `claim.timeline`, tail from `returnShipment.timeline`). Warranty's tail was relabelled to match — `ship_back` short `Ship back → Shipped`, `device_returned` `Returned → Delivered` (+ headline/label, and `ClaimDetailsSheet`'s "Returned on" → "Delivered on"). The three near-identical dot strips (`ClaimCard` / `WarrantyClaimCard` / `InvalidClaimCard`) were extracted into one shared tone-aware `src/components/ClaimProgressDots.jsx` (props `{ steps, curIdx, stamps, tone }`) so they can't drift; this also fixes `ClaimCard`'s old always-amber current-step glow to be tone-aware. Hero copy aligned too: the invalid paid hero label `Delivery by → Back with you by`, body "…like any other order" → "…like any other delivery", plus the `{claimRef} · Claim · {type}` subline the warranty hero already carries. `src/lib/claims.js`, `src/components/{ClaimProgressDots,ClaimCard,WarrantyClaimCard,InvalidClaimCard,ClaimDetailsSheet}.jsx`. See `docs/output/returns/claim_tracking.md` §3.3 + `warranties_compensations.md` §2.3.
- **Invalid/return claim now turns green and drops to Past once the device is delivered back.** The `InvalidClaimCard` paid card used to stay brand-purple with a future-tense `Back with you by {ETA}` hero even after delivery and linger in **In progress** (its `claimStatusId` never reaches a refund terminal). New `isReturnDelivered(order)` (`invalidClaim.returnShipment.currentStatusId === 'delivered'`, `lib/claims.js`) flips the whole card to success-green — left strip, `Delivered` state pill (Check), green hero with a `Delivered on {date}` strip, green dots — mirroring `WarrantyClaimCard`'s `device_returned`, and routes it to **Past orders** (`isOpen` returns false; new shared `isDeliveredPast` helper in `App.jsx` counts it toward the `delivered` chip), parity with `isWarrantyDelivered`. The `claim_invalid_return_delivered` journey node gained `deliveredOn`/`deliveredOnLong` for a clean delivered date. `src/lib/claims.js`, `src/App.jsx`, `src/components/InvalidClaimCard.jsx`, `src/data/journeys/claimIssue.js`. See `docs/output/returns/claim_tracking.md` §2.5 + §3.3.
- **Unified the device→customer return-leg events onto one trigger family.** Warranty's repair-return already fired `claim.ship_back.*` + `claim.device.returned`; the invalid / cancel paid-return fired a parallel `claim.return_shipping.paid` + `claim.return_shipment.*` set across the issue / warranty / change-of-mind journeys. Renamed the latter to the former (`return_shipping.paid → ship_back.created`; `return_shipment.{arrived_destination,cleared_customs,forwarded_to_agent,out_for_delivery} → ship_back.*`; `return_shipment.delivered → device.returned`) so one event keys the comm for every return leg, regardless of why the device is going back. **Node ids and `next` forks are unchanged** (still `claim_return_shipping_paid` etc. — those are graph addresses, not triggers). The two existing notification entries were re-keyed onto the unified names (no new copy authored), so they now fire for the warranty repair-return too; the four transit milestones stay `silent`. `src/data/journeys/{claimChangeOfMind,claimIssue,claimWarranty}.js`, `src/data/notifications/claims.js`. See `docs/output/journey_backend_spec.md`.
- **Harmonized the two reset-resubmission comms onto one event.** The `claim_reset_retry_resubmitted` node (second unlock-details submission) now fires `claim.reset.details_received` — the same event as the first submission — instead of its own `claim.reset.retry_resubmitted`, since both are the same customer-facing "we got your details" moment (`resetUnlock.attempt` 1 vs 2 still distinguishes them in card state). One notification entry now covers both; `claim.reset.retry_resubmitted` is retired (dropped from the `claims.js` scaffold). Node ids and `next` forks are unchanged. `src/data/journeys/{claimChangeOfMind,claimIssue,claimWarranty}.js`, `src/data/notifications/claims.js`. See `docs/output/journey_backend_spec.md`.
- **Notifications gained coverage metadata (`status`) + a journey roll-up.** Each notification now carries a `status` flagging how it relates to our existing comms — `live` (unchanged from prod), `new` (not sent today), `changed` (revising wording), `missing` (a gap, opt-in via a bare `{ status: 'missing' }` marker with no copy), and the default `silent` (intentional no-comm for any event with no entry). `JourneyNotificationPanel` shows it as a header badge and splits the empty state (`missing` → red "gap to fill" vs `silent` → muted "No notification sent"); `JourneyDevPanel` shows a "Comms" roll-up counting distinct events per status across the journey. `notificationFor` now always returns an object (`{ status, whatsapp, email }`, copy null when absent) instead of `null`; new helpers `notificationStatus(event)` + `journeyNotificationCoverage(nodes)` + `NOTIFICATION_STATUSES`. Convention is to tag every authored entry — `order.created` is the worked example, the rest default to `live` until tagged. To fit the new badge, the replay panel stack widened (`w-[360px]` → `w-[400px]`, `src/App.jsx`) and the WhatsApp/Email channel toggle is now icon-only (label kept as tooltip/a11y). `src/lib/notifications.js`, `src/components/{JourneyNotificationPanel,JourneyDevPanel}.jsx`, `src/App.jsx`, `src/data/notifications/orders.js`. See `docs/output/journey_backend_spec.md`.
- **Issue & warranty journeys gained a "Proof accepted" intake beat.** Because both carry proof (Step 2 attachment), submit no longer goes straight to pickup: `claim.created` (form-received, "we're reviewing it") → new `claim_proof_accepted` node firing `claim.documents.accepted` (the same pending-collection "courier will arrange pickup" notification change-of-mind gets at `claim.created`) → pickup outcome fork. The submit and `claim_docs_resubmitted` nodes now re-merge through `claim_proof_accepted` instead of pointing at pickup directly; the node's `apply` is identity (notification beat only, no card-state change). The pending-collection copy is shared via a `PENDING_COLLECTION` const so the two beats stay word-for-word identical to the screenshot. Change-of-mind (no proof) and compensation (no pickup) are unchanged. `src/data/journeys/{claimIssue,claimWarranty}.js`, `src/data/notifications/claims.js`. See `docs/output/journey_backend_spec.md`.
- **`claim.created` notification now diverges by claim type.** Claims that carry proof (issue/warranty/compensation) go into a background review → "we're reviewing it" copy + `/emails/claims/form-received.png`; change-of-mind has nothing to review and jumps to courier pickup → "courier will be in touch to arrange pickup" copy + `/emails/claims/pending-collection.png`. No new journey node/event — the divergence is a presentation-layer variant: the `claim.created` entry in `src/data/notifications/claims.js` now holds `variants: { proof, no_proof }`, and `notificationFor` picks one via the new `claimRequiresProof(claim)` helper (`src/lib/claims.js`). Also fixes the entry's dangling `claim-created.png` ref (the asset never existed). `src/lib/{claims,notifications}.js`, `src/data/notifications/claims.js`. See `docs/output/journey_backend_spec.md`.
- **Fixed: journey notifications (and the dev-panel current-state label) now resolve the current node by id, not array index.** On branched journeys the current node is the **last entry on the visited path**, not `nodes[path.length - 1]`. Sub-branch nodes are appended at the end of the nodes array (e.g. `claim_pickup_failed`, placed last "so default-next routing isn't disturbed"), so `nodes[currentIndex]` pointed at an unrelated node — the `JourneyNotificationPanel` showed the wrong event's message and `JourneyDevPanel`'s current-state label/event were wrong, while the Next buttons (driven by `currentNodeId`) stayed correct, so the two disagreed. Both now look the node up via `nodes.find(n => n.id === currentNodeId)`. `src/App.jsx`, `src/components/JourneyDevPanel.jsx`.
- Added the `claim.pickup.failed` customer notification (WhatsApp + Email, screenshot `/emails/claims/collection-failed.png`) to `src/data/notifications/claims.js` — the first wired claim-domain message beyond the `claim.created` example; surfaces on the Pickup-failed node of the change-of-mind / issue / warranty journeys.
- **Restructured notification content into per-domain data files + token-based copy.** The flat builder-function map in `src/lib/notifications.js` is now split into `src/data/notifications/*` — one file per `event.*` domain (`orders.js` for `order.*` incl. cancellation, `shipment.js` for the outbound leg, `claims.js` for `claim.*`), merged by `index.js` into `NOTIFICATIONS` (same barrel pattern as `data/journeys` / `data/orders`). Copy is now **pure data** — plain `{ whatsapp, email: { subject, body, screenshot } }` entries with `{token}` placeholders (e.g. `{trackingNumber}`, `{deliveredOnLong}`) instead of JS functions, so it's editable without touching code; `lib/notifications.js` keeps only `notificationFor` + a new `interpolate` helper. Email screenshots are reorganised into mirrored domain folders `public/emails/{orders,shipment,claims}/` (was flat). `claims.js` ships as a scaffold (one filled `claim.created` example + a commented index of the ~33 claim events) ready to fill as each flow's notifications are defined. Happy-path copy + behaviour unchanged. `src/data/notifications/{index,orders,shipment,claims}.js`, `src/lib/notifications.js`, `public/emails/`. See `docs/output/journey_backend_spec.md`.
- **Customer notifications in journey mode (happy path).** A new `JourneyNotificationPanel` stacks directly above the `JourneyDevPanel` (replay journeys only) and previews the WhatsApp / Email message the customer receives at the current node — a channel toggle switches between a short WhatsApp bubble and an email subject + body (distinct copy per channel), and the selected channel persists as you advance. Content is a new source-of-truth map in `src/lib/notifications.js` keyed by backend `event` name (`event → (order) => { whatsapp, email }`, via `notificationFor(event, order)`) so copy interpolates live order fields (tracking number, delivered date) and the same event reuses one message across journeys. Scope is happy-path **milestones only** (`order.created`, `order.quality_check.started`, `shipment.arrived_destination`, `shipment.out_for_delivery`, `shipment.delivered`); intermediate logistics steps (cleared-customs, forwarded-to-agent) and all not-yet-authored events fall through to a "No notification sent at this step" empty state. The Email preview is tappable and opens the **real Revibe email render full-size** in a portal lightbox (scrim + Escape-to-close) — paths are wired via `email.screenshot` (`/emails/<step>.png`, see `public/emails/README.md`) and the lightbox falls back to a "coming soon" placeholder until the image is dropped in. `JourneyDevPanel` lost its own fixed-position classes (the new stacking wrapper in `App.jsx` owns them); sandbox journeys (`EddSandboxPanel`) are untouched. `src/lib/notifications.js`, `src/components/{JourneyNotificationPanel,JourneyDevPanel}.jsx`, `src/App.jsx`, `public/emails/`. See `docs/output/journey_backend_spec.md`.
- **Warranty journey now exercises the docs-rejected takeover.** Added a pre-pickup docs-rejected fork to `CLAIM_WARRANTY_NODES`, mirroring the issue journey: `claim_submitted_warranty` branches into `claim_docs_rejected` (sets `claim.docsRejection` + `subStatusId: 'awaiting_documents'` + `actionRequired`, routing to the shared `DocsRejectedCard` ahead of `WarrantyClaimCard`), and `claim_docs_resubmitted` clears the takeover, sets `claim.proofResubmission` (surfaces the `Evidence resubmitted` chip in `WarrantyClaimCard`'s `HistoryThread`), and re-merges into the pickup outcome fork. Data-only — routing already supported `docsRejection` precedence; takeover copy stays the shared "Return claim" wording. `src/data/journeys/claimWarranty.js`. See `docs/output/journey_backend_spec.md` + `warranties_compensations.md` §2.6 + §2.9.
- **`ResetFailedCard` ("device still locked") now links to the guided reset's remote path** instead of its own inline iCloud step list. The expanded card swaps the three-step `Remove device from your iCloud` instructions card for a brand-toned `Run the guided remote reset` launcher that opens the shared `ResetGuideSheet` straight onto its remote route (new props `initialRoute="remote"` + `skipDone` — skips the intro fork and the "ready to ship" done screen, since the device is locked and already at Revibe; backing out of step 0 closes the sheet). The guide is device-typed via `deviceTypeForOrder`. Completing it unlocks the `I've removed this device from my iCloud account` confirmation toggle (locked/dimmed until then, mirroring Step 3's guide-unlocks-confirm pattern); the **device passcode gate is unchanged**. `src/components/ResetFailedCard.jsx`, `src/components/ClaimFlow/ResetGuideSheet.jsx`. See `docs/output/returns/claim_tracking.md` §3.4 + §3.4.1.
- Raise-a-claim copy: confirmation screen heading is now a single **"Your request has been submitted"** for all claim types (was a per-type "Your warranty claim / compensation request / return request is in"); the type chip below it is unchanged. Removed the "Each step has its own SLA …" sub-line under the pickup-confirm checkbox on the Pickup address & contact step. `src/components/ClaimFlow/{Step7Confirmation,Step4PickupDetails}.jsx`.
- **Step 3 device prep — "never set up" skip + consolidated off-ramps + leaner guide finish.** Added a **change-of-mind-only** escape hatch: a device that was never set up has no account linked and nothing to erase, so the customer can attest to that instead of running the guided reset. New `devicePrep.neverSetUp` flag — `stepError`'s `deviceprep` case short-circuits to satisfied when `claimType === 'change_of_mind' && neverSetUp` (gated by claimType; `ROUTE_FROM_REASON` clears it when routing off the change-of-mind track); ticking it dims the `HeroLauncher` and hides the `SafetyNote` + reset `ConfirmGate` (the attestation becomes the gate). It rides into the seeded claim and is read before the `option === 'reset'` branch in `devicePrepText` ("Not set up — no reset needed"), `Step6Review`, and `Step7Confirmation`. The two reset off-ramps were **consolidated** into one collapsed-by-default `Can't run the guided reset?` disclosure (`ResetOffRamps`, bordered button row with `HelpCircle` + chevron) — replacing the always-on "Can't unlock or power on?" line — listing the change-of-mind skip first, then the informational remote-route row; the always-on "Run the guide and tap Done" hint under the confirm gate now shows only in the red error state. Separately, the `ResetGuideSheet` **done screen was simplified**: removed the optional pre-ship checklist (`FINAL_CHECKS` data, `SimCard` mock glyph, and the `resetGuideChecks` reducer/props plumbing — all dead), centered the "Your … is ready to ship" success message, and blanked the "Almost there" header title. `src/components/ClaimFlow/{Step3DevicePrep,ResetGuideSheet,resetGuideMocks,flowReducer,Step6Review,Step7Confirmation}.jsx`, `src/lib/claims.js`. See `docs/output/returns/guided_reset.md` §4–§5 + `change_of_mind.md` §2.4.

## 2026-06-08

- **Return reason is now required and routes the claim.** The change-of-mind "Why are you returning it?" step lost its `Skip` and is now a **required, shared early step** across the change-of-mind / issue / warranty flows (`Step2Reason` rendered for all three; compensation keeps its own first step). The curated reason list (Amazon-parity, reworded to Revibe voice, `Other` last) mixes genuine change-of-mind reasons with four fault/wrong-item "catch" reasons (`defective` / `wrong_item` / `damaged` / `missing_parts`). The reason is the **authoritative router** (`routeForReason` in `Step2Reason.jsx`): a no-fault reason → change of mind; `defective`/`wrong_item`/`damaged` → issue (or **warranty when the order has Revibe Care**, only when caught in the change-of-mind flow — inside issue/warranty they just continue); `missing_parts` → compensation. When the resolved track differs from the current flow, **Continue opens an authoritative `SwitchFlowSheet`** (new `src/components/ClaimFlow/SwitchFlowSheet.jsx` — bottom sheet w/ X, "Switch to {track}" vs "Choose another reason", no continue-anyway) so a faulty/wrong/incomplete device can't proceed down the no-fault change-of-mind track. Routing into issue/warranty pre-fills the matching issue-details scope. The flow reducer was refactored from fixed integer steps + per-type skip arithmetic to explicit per-type **`STEP_SEQUENCES`** of string step-keys (`state.step` is now a key; `NEXT`/`BACK`/`GO_TO_STEP` walk the active sequence; new `ROUTE_FROM_REASON` action); visible step counts: change-of-mind 8, issue 9, warranty 8, compensation 5. `src/components/ClaimFlow/{Step2Reason,SwitchFlowSheet,ClaimFlow,flowReducer,Step6Review}.jsx`, `src/lib/claims.js` (`REASON_LABELS`). See `docs/output/returns/change_of_mind.md` §2.3, `issue.md` §2.2, `warranties_compensations.md` §2.4 + §3.3.
- **Harmonized the two return-shipment surfaces** (`InvalidClaimCard` paid state + `WarrantyClaimCard` ship-back). Extracted the return-leg `See detailed tracking` dropdown — brand-toned toggle, DHL/AWB courier strip with copy, outbound `SHIPPING_SUB_STATUSES` sub-timeline — into one shared `src/components/ReturnShipmentTracking.jsx` (exports `ReturnShipmentTracking` + the reusable `CourierStrip` / `SubStatusItem`), so the two cards render the return leg identically and can't drift. `InvalidClaimCard`'s paid-state dropdown was previously neutral-toned with no courier strip; it now matches the warranty card (gate relaxed from `currentStatusId === 'shipped' && subStatusId` to just dispatched, and the invalid `returnShipment` mocks gained an `awb` + opening sub-status so the strip is complete). Removed the decorative icon-only **`Download receipt`** button from `WarrantyClaimCard`'s footer (`View claim details` now fills the row, mirroring `PastOrderCard`'s refund-hero). The warranty card's **inbound** (customer → Revibe) `PickupTransitDetail` dropdown is now gated to the pre-return window (`!claim.shipBack?.awb`), so only one detailed-tracking dropdown shows at a time — inbound while the device travels to Revibe, the return leg once the ship-back AWB exists. `src/components/{ReturnShipmentTracking,WarrantyClaimCard,InvalidClaimCard}.jsx`, `src/data/orders/claims.js`, `src/data/journeys/claimWarranty.js`. See `docs/output/returns/claim_tracking.md` §3.3 + `warranties_compensations.md` §2.3.3.
- The collapsed-state `Tap to fix` affordance on the four **Action needed** takeover cards (`DocsRejectedCard` / `PickupFailedCard` / `ResetFailedCard` / `InvalidClaimCard`) is now a full-width **solid danger button** (`bg-danger` white text + trailing arrow) instead of a faint muted-grey text line, matching the app's existing danger CTAs. Extracted to a shared `src/components/TapToFixCta.jsx` so the four can't drift. It stays a non-interactive styled element (the whole card header is still the single tap target — convention preserved); the parent header carries a `group` class so the CTA gets a press-scale. Copy unchanged; collapsed-only. `src/components/{TapToFixCta,DocsRejectedCard,PickupFailedCard,ResetFailedCard,InvalidClaimCard}.jsx`. See `docs/output/returns/claim_tracking.md` §3.
- **Cancel claim — post-collection ship-back path.** Cancelling once the device is already with Revibe no longer reverts to Delivered (which wrongly implied no pickup happened); it now routes to a pay-return-shipping flow to get the device back. `canCancelClaim` widened to the whole pre-completion window (`initiated`/`pickup`/`qc`); new `cancelNeedsShipBack(claim)` picks the path — clean revert for pre-pickup device claims + all compensation, ship-back for device claims at `pickup`/`qc`. The ship-back path reuses `InvalidClaimCard` wholesale via a new `claim.invalidClaim.reason` discriminator (`'invalid'` default vs `'cancelled'`): same fee card, delivery details, `Pay {amount}`, paid-state return-shipment tracking, and journey return chain — only the copy switches (label `Cancelled claim`, "Pay return shipping to get your device back", neutral system note) and the secondary action swaps `Decline` → **`Keep claim`** (resume). `cancelReturnGate(order)` builds the `reason: 'cancelled'` gate; App overlays it via a new `shipBackCancels` map (clearing any takeover flag so routing lands on `InvalidClaimCard`). `CancelClaimSheet` copy is now path-aware (two-step "Continue to payment" for ship-back). Journeys: new `claim_cancelled_shipback` node on the three device journeys, wired into `claim_qc_started` + `claim_reset_failed`, pointing into the existing `claim_return_shipping_paid` return chain. So the `ResetFailedCard` (`qc`) cancel now correctly pays-to-return rather than reverting. `src/lib/claims.js`, `src/App.jsx`, `CancelClaimSheet.jsx`, `InvalidClaimCard.jsx`, `src/data/journeys/claim{ChangeOfMind,Issue,Warranty}.js`. See `docs/output/returns/claim_tracking.md` §2.8 + §3.3.
- New **`Cancel claim`** affordance lets a customer cancel an in-flight claim before anything irreversible happens. Single source of truth `canCancelClaim(claim)` in `src/lib/claims.js`: device claims (`change_of_mind` / `issue` / `warranty`) are cancellable only at `initiated` (before the courier collects the device — once it leaves for `pickup` it's in transit); compensation has no shipment leg, so it stays cancellable through `qc` ("Under review") until the refund is issued. Surfaced as a persistent danger-tinted text button in a bordered footer strip on `ClaimCard` + `WarrantyClaimCard` — **visible whether the card is collapsed or expanded** (sits below the header, outside the expand block). The three takeover cards' previously-stubbed `Cancel claim` buttons (`PickupFailedCard` / `DocsRejectedCard` / `ResetFailedCard`) are now wired to the same action. Tapping opens a confirmation bottom sheet (`CancelClaimSheet.jsx`, `CancelOrderSheet` chrome — scrim, slide-up, Escape, body-scroll lock) with a "what happens" recap and `Keep claim` / `Cancel claim`. Confirm marks the order in App's `cancelledClaims` map; the projection strips the claim (winning over `submittedClaims` + hand-seeded claims), reverting the order to its delivered `PastOrderCard` (re-raisable), undoable via the generalized `UndoSnackbar` ("Claim cancelled"). In **journey mode** cancellation is a real terminal node — a `claim_cancelled` end node added to all four claim journeys, wired into every cancellable node's `next` (the `claim_submitted_*` nodes + compensation's `claim_under_review`, matching the `canCancelClaim` window); confirming the sheet advances to it (also a dev-panel valid-next button) and its `apply()` strips the claim → order reverts to delivered. Journey takeover surfaces (no `claim_cancelled` branch) fall back to the in-session `cancelledClaims` revert, cleared on the next node change. `InvalidClaimCard` keeps its own `Decline` terminal (out of scope — post-shipment). `src/lib/claims.js`, `src/App.jsx`, `src/components/CancelClaimSheet.jsx` (new), `ClaimCard.jsx`, `WarrantyClaimCard.jsx`, `PickupFailedCard.jsx`, `DocsRejectedCard.jsx`, `ResetFailedCard.jsx`, `src/data/journeys/claim{ChangeOfMind,Issue,Warranty,Compensation}.js`. See `docs/output/returns/claim_tracking.md` §2.8 + `warranties_compensations.md` + `journey_backend_spec.md`.
- **Product line-item redesign** — the "what you bought + what you paid" row is now one shared `ProductSummary` component (`src/components/ProductSummary.jsx`), replacing ten near-identical inline copies across the order/claim cards and the twelve duplicated `REVIBE_CARE_ICON` constants (now a single export, imported by the cards + `CancelOrderSheet` / `Step5RefundMethod`, mirroring `WalletInfoTooltip`'s `REVIBE_WALLET_ICON`). Design: `docs/handoff/product-summary/design.md` ("Direction C — Elevated Care"). New treatment: a larger **72px** product thumbnail on a clean white tile; **Revibe Care promoted to a dedicated callout carrying the brand hero gradient** (magenta/violet glow + white icon chip + "extended warranty" subline + `+{amount}`); and an explicit price breakdown — `Device` (subtotal) top-right, `+Revibe Care`, and a bold **Total paid** on a divider — so `subtotal + warranty = total` reads with no mental math. `tone="light"` (9 cards) vs `tone="hero"` (`HeroCard`, where the callout inverts to a frosted-white panel). No-warranty orders collapse to product + a single `Total` (callout + divider omitted). Integration deltas: `InProgressCard` / `ClaimCard` / `WarrantyClaimCard` / `PastOrderCard`'s cancelled refund-hero card had their decorative chevrons relocated from the row to the `Order · #{id}` header; `HeroCard`'s translucent strip wrapper dropped (the gradient callout renders directly on the hero). The cancelled refund-hero card uses the full `ProductSummary` too — its `RefundHero` shows the refund amount, the row shows the device + Revibe Care + **Total paid** (original price), distinct whenever a cancellation fee applies; both reuse the order's own fields (carried from `INITIAL_ORDER` in journey mode), no per-card hardcoding. Care subline copy ("2-year extended warranty · added") is a placeholder pending real Revibe Care wording; the bigger-image lever is parked (design.md §6). `src/components/{ProductSummary,OrderCard,InProgressCard,PastOrderCard,HeroCard,ClaimCard,WarrantyClaimCard,DocsRejectedCard,InvalidClaimCard,PickupFailedCard,ResetFailedCard,CancelOrderSheet}.jsx`, `src/components/ClaimFlow/Step5RefundMethod.jsx`. See `docs/output/orders.md` §3.0.
- New **late + past-promise cancellation** branch: when an order has blown its SLA *and* is past the initial delivery promise (at `created` / `quality_check`), cancelling waives the 5% card processing fee (full refund) and adds a flat **AED 100 Revibe Wallet bonus** on the store-credit path — mirroring the issue/wrong-device claim's bonus. Driven by a single stamped `order.promiseBreached` flag (production derives it from the EDD model: current-stage late AND `today > initialPromise`). `CancelOrderSheet` reads the flag: Select-step Wallet option shows the bonus as the issue-claim's **purple `accent` pill** (`bg-accent/15 text-accent`, `Sparkles`) + a "Full refund + bonus" line, the card option drops the fee line ("No cancellation fee"); the dissuade step is **repurposed into an apology + Keep-my-order screen** ("We missed our delivery estimate", no-fee + bonus nudge) on the original-payment path; Confirm reflects the waived fee / +bonus. `refund.bonus` surfaces on the refund-hero (`PastOrderCard`) and inside `RefundDetailsSheet`. Added to the **Cancellation journey** as `order_late` (before-QC) + `qc_late` (at-QC) entry nodes, each forking into waived-fee / wallet-bonus cancel branches; `App.handleCancelOrder` routes to the new `cancel_late_*` / `cancellation_late_requested_*` nodes. Two static demo orders added (`89720` in-flight late-at-QC; `89205` settled wallet-bonus refund). `src/components/CancelOrderSheet.jsx`, `RefundDetailsSheet.jsx`, `PastOrderCard.jsx`, `src/App.jsx`, `src/data/journeys/cancellation.js`, `src/data/orders/baseline.js`. See `docs/output/cancellations.md`.
- Delayed in-progress orders now show an **amber** accent in the `InProgressCard` delivery hero — the right-side `Clock` + "Taking longer than expected" tag turns `text-amber-600` when `order.delayed` (the hero gradient, ETA headline, and state pill stay brand-purple; "accents only"). This retires the prior "delayed QC stays fully brand-purple" rule: the delay signal is now amber-tinted, not purple. The shared `warn` token is untouched, so `OrderCard`'s shipped-late banner keeps its existing tone. Affects the EDD-sandbox late `created`/`QC` states and the one baseline delayed-QC order. `src/components/InProgressCard.jsx`. See `docs/output/orders.md` §8.
- Dynamic-EDD sandbox banner copy now **reuses the card copy** for the happy path instead of its own parallel strings. The on-time `created`/`quality_check`/`shipped` states and `delivered` no longer set `order.statusBanner` (it's left null), so `statusDescription` resolves tone/lead/body from `STATUS_DESCRIPTIONS` in `statuses.js` — the same copy the non-journey cards and event-driven journeys show. The sandbox-owned `statusBanner` override now fires **only** for the SLA-divergence messages (`order_late` / `qc_late` / `shipped_late` / `qc_back_on_track`), which have no card equivalent. Removes the now-duplicate `MSG_ORDER_ON_TIME` / `MSG_QC_ON_TIME` / `MSG_SHIPPED_ON_TIME` / `MSG_DELIVERED` constants from `edd.js` (their `buildCustomerMessage` entries return `body: null`; the debug panel reads only the message `key`). Fixes the happy-path inconsistency where the sandbox said "Order received. Quality check will begin shortly." while the cards said "We've received your order and it's on its way to our quality lab for inspection." `src/lib/edd.js`, `src/lib/eddSandbox.js`. See `docs/output/journey_backend_spec.md`.
- Cancellation `refund_pending` status banner now gives a concrete refund window ("typically lands within 5–7 business days, depending on your payment method") instead of "will land shortly" — aligns the pending-state promise with the already-specific refunded-state copy. `src/lib/statuses.js`.
- Agent-collaboration infra (no UX change). **P4** in `collaboration_strategy.md` done: new `npm run freshness` (`scripts/freshness.mjs`) — report-only weekly/ad-hoc check that regenerates `code_map.md`, then flags which `docs/output/*` docs have source changes since their `verified_against` marker (`git log <marker>..HEAD -- <covers>`), warns on dead `covers` paths, and writes a gitignored `docs/process/freshness-report.md` + stdout summary. Seeded the `status` / `verified_against` / `covers` frontmatter (specified in P5, not previously applied) into all eight `output/*` docs. Added a **Subagent discipline** section to `CLAUDE.md` (explore-sparingly + seed-with-code_map-rows; Plan agents state blast radius from the consumers/coupling tables). README freshness bullets updated. First freshness run also caught real pre-existing drift — `code_map.md` had a stale `PastOrderCard.jsx` LOC from the prior commit — now regenerated.
- Removed the decorative icon-only `Download receipt` button from the refund-hero card's footer (`PastOrderCard` cancelled branch); `View refund details` now fills the row full-width. See `docs/output/cancellations.md` §3.

## 2026-06-05

- Docs reorg (no UX change), **P5** in `collaboration_strategy.md`. `docs/` reorganised by read-cadence: backend-mapping briefs (`db_explorer_context.md` / `db_explorer_system_prompt.md`, read only by the separate backend-mapping project) moved to `docs/handoff/backend-mapping/` as `context.md` / `system_prompt.md`; `collaboration_strategy.md` moved to `docs/process/`; design-history `output/claim_detailed_tracking.md` deleted (its live tables already canonical in `output/returns/claim_tracking.md` §4). `input/` + `output/` names kept. New **Doc tiers & review strategy** section in `docs/README.md` (tiered loading + freshness frontmatter + trigger-based verification + opt-in confidence pass). Repointed all live cross-references (`README.md`, `claim_tracking.md`, `CLAUDE.md`, the moved system prompt).
- Agent-collaboration infra (no UX change). **P2 cross-cutting diagrams** done: new `docs/output/diagrams.md` with three hand-maintained mermaid diagrams — card-routing decision tree (faithful to `App.jsx`'s two-stage `isOpen` partition + per-section precedence ladder), unified claim-lifecycle (refund / compensation / warranty pipelines + the four takeover detours on one canvas, annotated by card + trigger state), and the returns submit→seed→project→render data-flow (the runtime projection no import edge shows). Linked from `code_map.md` (new `Cross-cutting diagrams` section) + `docs/README.md`; `collaboration_strategy.md` P2 marked done.
- Agent-collaboration infra (no UX change). New `docs/code_map.md` — navigation + impact layer (concept→file→line index, string-contract coupling table, generated dependency/blast-radius map) rebuilt by `npm run codemap` (`scripts/codemap.mjs`); `CLAUDE.md` + `docs/README.md` now point agents to it before exploring. New `docs/collaboration_strategy.md` roadmap (P0 code map → P1 splits → P2 diagrams → P3 slim CLAUDE.md → P4 subagent discipline). **P1 file splits** (behavior-preserving, byte-verified): `data/journey.js` 3188→ barrel + `data/journeys/*` (per journey); `data/orders.js` 1348→ barrel + `data/orders/*` (per scenario); `ResetGuideSheet.jsx` 2477→ shell + `resetGuideMocks.jsx` (CSS-art screens) + `resetGuideAnim.js` (shared primitives). Public exports unchanged (`ORDERS`, `JOURNEYS`, `INITIAL_ORDER`, default `ResetGuideSheet`).

## 2026-06-03

- Step 3 guided reset gains a dedicated **Android-tablet variant** (was: Android tablets reused the Samsung-phone guide). The `TabletPicker` options are trimmed to just **Apple** / **Android** (sub-labels dropped), and the Android pick now resolves to a new `device: 'android_tablet'` (distinct from the `android` Samsung-phone guide) carrying its own `COPY` / `DEVICE_STEPS` / `REMOTE_STEPS` / `FINAL_CHECKS` / `DEVICE_LABEL` with "tablet" wording. The six Samsung mocks are now `Frame`-parametrised (new android-tablet wrappers bake in `MiniTablet`); the two remote screens take a `tablet` flag for the `Tablet` icon + "This tablet" / "Galaxy Tab" copy; `IntroDevice` reuses the Apple `IntroTablet` slab. `MiniTablet` was widened to a true ~3:4 portrait-tablet aspect (300×384 inner, no upscale — the phone's 1.32× upscale had left the two frames near-identical in width, so it read as a chunky phone); **this also widens the Apple iPad guide's frame.** The Google "Your devices" overflow menu was rebuilt from a fixed 96px right-floated popover (overflowed the bezel in the wider frame) into a full-width contained Manage/Sign out menu matching the Apple iCloud Erase/Remove pattern, and the One UI mock text got explicit `text-left` so it no longer inherits the step container's `text-center` in the wider frame. `Step3DevicePrep`'s launcher step-count keys off `os === 'android'`. See `docs/output/returns/change_of_mind.md` §2.4 + `CLAUDE.md`.
- Step 3 guided reset gains **iPad and MacBook variants**, and the platform model splits into two dimensions. `src/lib/devices.js` adds `deviceTypeForCategory`/`deviceTypeForOrder` (→ `iphone | ipad | mac | android | tablet`) alongside the existing `os` resolvers; `ResetGuideSheet` is now **`device`-parametrised** (was `os`). New full-fidelity walkthroughs: **iPad** reuses the iOS step content in a new `MiniTablet` shell with iPad wording (`appleRemoteSteps('iPad', MiniTablet)` for the remote path); **Mac** gets its own `MiniLaptop` shell + macOS System Settings mocks (sign out → Erase All Content and Settings → Setup Assistant) and a desktop-Safari remote path (`MacBrowserBar` + `MacICloudRemove`/`MacAccountRemove`). The account-removal carousel is now built per-device (`makeAccountCarousel(noun, Frame)` + `DEVICE_META`) and `ICloudRemove`/`MockCarousel` take a `Frame` so iPad/Mac render in the right shell. New CSS intro illustrations `IntroTablet`/`IntroLaptop`; `IntroDevice` branches on `device`. `Step3DevicePrep`'s "simple steps" chip is now dynamic. See `docs/output/returns/change_of_mind.md` §2.4.
- Step 3 guide platform realigned to **real production categories** and gains a scoped manual input for the OS-ambiguous `Tablet` category. `osForCategory` now checks Android before Apple; `deviceTypeForCategory` maps `Macbook` → mac, `Tablet` → the ambiguous `tablet` type (new `isOsAmbiguous(order)`). Because the production `Tablet` category covers both iPads and Android tablets, the reducer **preselects iPad** and `Step3DevicePrep` renders a `TabletPicker` segmented control ("What kind of tablet is this?" → Apple iPad / Android tablet); switching to Android sets `device`/`os` and re-gates the confirm checkbox. Android tablets reuse the Samsung phone guide. `Laptop` (Windows, distinct from `Macbook`) is left for later. Two seeded delivered mock orders added (`Tablet`, `Macbook`) to demo the chooser + Mac guide. `devicePrep` state gains a `device` field. See `docs/output/returns/change_of_mind.md` §2.4 + `CLAUDE.md`.

## 2026-06-02

- Journey-mode `cancel_at_qc` merged into a single `cancellation` journey (id + label renamed; the old `?journey=cancel_at_qc` URL no longer resolves). `placed` now forks into two cancellation entry points: a new **before-QC** branch (`created` stage) that skips supplier review and lands straight on `refund_pending` — stamping a `requested` timestamp so the stepper reads Requested ✓ → Pending, with no accepted/declined fork — and the existing **at-QC** branch (`quality_check`, unchanged: requested → accepted/declined). Each entry point forks on refund method (wallet/card). `cancellation_accepted_*` nodes gained explicit `next` (no longer rely on array adjacency). The cancel sheet's `handleCancelOrder` is now stage-aware — advances whichever request node is in `validNext()` (before-QC vs at-QC). See `docs/output/journey_backend_spec.md`.
- `I want to keep my order` (reverse cancellation) is now wired in journey mode. New `cancellation_kept` node, reachable only from `requested`: flips `state` back to `open`, voids the in-flight `refund`, clears `cancellationStatusId`, leaves `statusId` (`quality_check`) untouched, and re-merges into the shipping chain so the kept order resumes to delivery. `KeepOrderSheet` gained an `onKeep` callback (threaded `App.handleKeepOrder` → `PastOrderCard` → `CancelledOrderCard`); the keep window narrowed from `requested`/`refund_pending` to **`requested` only** (`canKeep`). A `cancellationTimeline.reverted` timestamp + `cancellationReversal` marker survive so a neutral `Cancellation reversed` chip surfaces in the `HistoryThread`; `getHistoryEvents` gained a `reverted` event and now surfaces cancellation traces in `delivered` mode too. See `docs/output/cancellations.md` §4 + §7.3.
- Customer-facing copy now spells out "quality check" instead of the acronym "QC": the battery self-check estimate caveat, the issue-picker subtitle, the description inline error, and the in-range-battery body in `Step2IssueDetails`, plus the claim-timeline dot label in `claims.js` (`short: 'QC'` → `'Quality Check'`, matching the orders-timeline convention). Dev-panel labels and internal code constants left as-is.
- Step 3 (Device prep) guided reset is now **category-driven, not user-chosen** — the `iPhone` / `Android` OS-tabs picker (`OsTabs`) is gone. New `src/lib/devices.js` resolves the guide platform from the order's `category_name` (`osForCategory`: iPhone/iPad/Apple → `ios`, Samsung/Android/Galaxy → `android`, unknown → `ios`; `deviceOsForOrder` falls back to `order.deviceOs` only when `category_name` is absent). `flowReducer` initialises `devicePrep.os` from `deviceOsForOrder(order)` (was hardcoded `'ios'`, which had masked `order.deviceOs` for Android orders); `Step3DevicePrep` reads `os` straight from state. `category_name` added to `INITIAL_ORDER` (`'iPhone'`); the **change-of-mind journey** order reseeded as a `Samsung Galaxy S21` / `category_name: 'Samsung phone'` to demo the Samsung guide end-to-end. See `docs/output/returns/change_of_mind.md` §2.4.
- Step 3 (Device prep) restyled to the **"Refined card" design direction** (implements the `device-prep/Device Prep Redesign.html` handoff). `Step3DevicePrep.jsx`'s presentation rebuilt — same props, `SET_DEVICE_PREP` dispatch, `ResetGuideSheet` integration, and `resetGuide`/`resetConfirm` error semantics: the `Start the guided reset` launcher is now an **elevated hero** (gradient icon coin with inner highlight rim, decorative concentric brand rings, a `~10 min` + `3 simple steps` meta-chip row, trailing `ArrowRight` affordance circle) whose tone tracks state (brand default → success `done` with `Check` + `RotateCcw` "run it again" → danger `error` with `AlertTriangle` + `animate-shakeX`); a new `SafetyNote` reassurance line (*"Worried about your photos?"*, `ShieldCheck`, reads iCloud/Google by OS) sits under it; the warn callout gains a circle badge; and the confirm checkbox is now a **bordered card** (border/fill track locked → error → checked → default, `Lock` glyph while locked). Added `shakeX` keyframe + `animate-shakeX` to `tailwind.config.js`. `ResetGuideSheet` untouched. See `docs/output/returns/change_of_mind.md` §2.4.
- Step 3 (Device prep) collapsed to a **single mandatory path**: run the guided reset, then tick the confirm checkbox. The second option — *"I can't factory reset it"* (the unlink toggle + 6-digit passcode credentials path) — was removed, since the guided reset's own remote route already covers customers who can't physically unlock or power on the device. `Step3DevicePrep.jsx` rewritten: the radio `OptionCard`s, `UnlinkPanel`/`UnlinkToggle`/`PasscodeField` and their guide data are gone; the screen now shows the warn callout, OS tabs, a single prominent `Start the guided reset` launcher card, a `Can't unlock or power on the device?` remote-path helper line (WifiOff), and the confirm checkbox (copy now *"I confirm I've completed the guided reset — this device is unlinked and erased."*; error *"Confirm you've completed the reset before continuing."*). `flowReducer` drops the `devicePrepOption`/`unlink`/`passcode` `stepError` keys and the `credentials` branch from `canAdvance`; `devicePrep` state loses `accountUnlinked`/`passcode` and pins `option` to `'reset'` so downstream Review / Confirmation / `devicePrepText` keep rendering the reset branch. The dead `'credentials'` branches in those three remain only for the seeded mocks in `orders.js` that still carry `option: 'credentials'`. See `docs/output/returns/change_of_mind.md` §2.4.
- Step 3 (Device prep) **Android now gets the same guided reset as iOS** — `ResetGuideSheet` is OS-parametrised via an `os` prop (one shared shell — phase machine, full-screen portal, progress bar, back chevron, slide transitions, trouble disclosures, `MockCarousel`, `MiniPhone`/`Row` — with per-platform step tables, copy, and mock screens). Both OS tabs now launch the same `Start the guided reset` card; the static Android inline accordion (and the dead `RESET_GUIDE` data in `Step3DevicePrep.jsx`) are gone, and the guide-seen confirm gate (`resetGuideSeen`) applies to both platforms (`flowReducer` `stepError`/`canAdvance` dropped their `os === 'ios'` qualifier). The intro route-chooser copy is identical on both (*Yes, I can unlock it* / *No — it's broken or won't turn on*); the hero differs (iOS PNG vs an asset-free CSS Galaxy slab). **Android (Samsung One UI)** carries two locks — Google FRP + Samsung Reactivation Lock — so both routes have an extra account step: device route is **4 steps** (remove Google account → sign out Samsung + turn off Find My Mobile → remove screen lock + Factory data reset → restart & verify), remote route is **2 steps** (`myaccount.google.com` Sign out → `account.samsung.com` Remove). Step 2's mock is a new two-screen carousel (`SamsungSignOutCarousel`: Find My Mobile toggle → Samsung account detail with **Sign out** glowing). Full hand-built One UI mocks (white cards on light-gray, punch-hole camera, Samsung-blue toggles, brand highlight) reuse the iOS primitives; trouble disclosures cover forgotten Google password, One UI 6.1+ Identity Check biometrics, and post-reset account prompts. Done-screen final checks add microSD + Galaxy Watch/Buds. Source: `samsung-factory-reset-guide.md`. See `docs/output/returns/change_of_mind.md` §2.4.
- `ResetGuideSheet` **carousel steps now auto-advance**: on any step whose mock is a carousel (the iOS `account.apple.com` route, the Android Samsung sign-out step), tapping the footer `I've done this` walks the carousel one screen at a time and only advances the flow step once the last screen has been reached — the customer can't skip the sub-screens. The label stays `I've done this →` mid-carousel and becomes `— finish` only on the final screen of the final step. `MockCarousel` is now **controlled** (the sheet owns the index, reset on each step/route change; arrows, dots, footer, and the header back chevron all drive it); detection is via a `.screens` static on the carousel component (`AccountRemoveCarousel` / `SamsungSignOutCarousel`).
- Step 2 (issue / warranty raise-a-claim, `Step2IssueDetails`): the per-issue **`What we need`** ask + **`How to provide valid proof`** link moved out of the selected-sub-type panel and into the **Photo or video of the issue** section, rendered (new `ProofGuidance`) above the upload box once a sub-type is picked — guidance now sits where proof is attached. The selected-row panel keeps only the optional `Try this first` tip (and renders nothing when a sub-type has none). The proof link is now a real `<a target="_blank">`: resolves per sub-type via `sub.proofGuideUrl`, else `DEFAULT_PROOF_GUIDE_URL` ("how-to-show-us-your-issue"); specific articles wired for `battery`, `physical` (device-conditions), and the four hardware sub-types `camera`/`microphone`/`button`/`speaker` (shared `HARDWARE_PROOF_GUIDE_URL`) — all in `issueSubtypes.js`. New amber `PhysicalConditionNote` shows in the photo section when `issueSubtypeId === 'physical'` and `conditionGradeOf(order)` is known and below `excellent`: it names the grade and notes that expected signs of previous use aren't defects (hidden for Excellent / unknown grade). See `docs/output/returns/issue.md` §2.2.
- Raise-a-claim flow: **no more grayed-out Continue button on any step.** The button is now always clickable across all six gated steps (claim type, issue/compensation details, device prep, packing, pickup, refund method); a premature click surfaces the step's first unmet input inline (red `border-danger` field + shared `InlineError` message + `scrollIntoView`) instead of disabling the button — generalising the soft-validation that previously only covered the Step 7 Review acks and the iOS reset-guide gate. New `stepError(state)` in `flowReducer.js` is the single ordered source of truth for what lights up ("one at a time"); new shared `InlineError.jsx`. The attempted flag moved from component-local `useState` into the reducer (`state.attempted`, set by `ATTEMPT`, cleared atomically by `NEXT`/`BACK`/`GO_TO_STEP`/`SUBMIT`) — this fixes a one-frame red flash on the next step when advancing, which the old `useEffect`-based reset caused by running after paint. `canAdvance` retained as a validity predicate but no longer drives the disabled state. See `docs/output/returns/change_of_mind.md` §2.1.1.
- Step 4 (Packing) gains a **packing-demo video** at the top of the step (`PackingDemo` in `Step4Packing.jsx`) — a brand-framed full-width square (`public/revibe_packing_guide.mp4`, 720×720, ~12s) that autoplays silently on loop (`autoPlay loop muted playsInline`) with a "Watch first" chip and a "Tap to expand" gradient bar; tapping opens a `createPortal` lightbox (`DemoLightbox`, Esc/backdrop/× close, body-scroll lock) replaying the clip full-width with native `controls`. The per-option instruction bullets were pulled off the two radio cards (cards now show just icon + title + subtitle) and replaced by a single collapsible **Packing tips** disclosure (`PackingTips`) below the options — a brand-tinted card with a filled-brand `Lightbulb` badge, collapsed by default, listing three general tips (`PACKING_TIPS`: nothing loose / seal seams / mark "Fragile"). Shared by the `change_of_mind` and `issue` branches; `PACKING_OPTIONS` lost its now-unused `bullets` arrays. See `docs/output/returns/change_of_mind.md` §2.5.

## 2026-06-01

- `ResetGuideSheet` remote route gains a **second step** — an `account.apple.com` device-removal walkthrough framed as *Or unlink it from your account* (alternative to the existing `icloud.com/find` step). Its mock is a new four-screen **swipeable carousel** (`AccountRemoveCarousel`): prev/next arrow buttons overlaid at the phone edges, clickable dot indicators, a per-screen `N. caption`, and the sheet's directional slide animation. Screens recreate the account.apple.com flow as polished CSS mocks (matching `ICloudRemove`'s style): account menu → **Devices** → pick this iPhone → scroll to **About** → **Remove from account** (glowing); account-holder details are masked. New step's "Which one should I use?" disclosure contrasts the two remote routes. `REMOTE_STEPS` goes 1 → 2 steps, so the remote progress bar now reads `Step X of 2`.
- Step 3 (Device prep) `Start the guided reset` launch card: removed the iPhone product shot (and its `mix-blend-multiply` image, edge-fade mask, brand glow, and `pr-12` spacing) — the card now shows just the icon + text.
- Step 3 (Device prep) iOS reset path reworked into a **guided, one-step-per-screen walkthrough** (implements the "Guided iPhone Reset" design handoff). `ResetGuideSheet.jsx` rewritten from the route-chooser-plus-checklists sub-sheet into a full-screen guided flow: an **intro** route-chooser (iPhone hero + "About 10 minutes" pill → *Yes, I can unlock it* / *No — it's broken or won't turn on*), per-step screens each grounded in a CSS-drawn **mock iOS screen** (Settings → Sign Out, Transfer-or-Reset → Erase, the Hello screen, and the iCloud Find Devices sheet — all light-themed), a segmented `Step X of N` progress bar, a back chevron, directional slide transitions, a brand/warn "why" callout per step, and a collapsible troubleshooting disclosure that can **escalate to the remote path**. The **done** screen has a success tick + an optional final-touches checklist (SIM, Apple Watch, IMEI photo, order number) persisted in `devicePrep.resetGuideChecks`. Two routes: **device** (3 steps) and **remote** (1 consolidated step — `icloud.com/find` → **Remove This Device**, *not* Erase; the why callout explains Erase keeps Activation Lock, and a "What if it's still online?" disclosure covers offline-vs-online + the 30-day re-link quirk). On Step 3 itself the iOS launch is now a `Start the guided reset` card (Sparkles icon, iPhone product shot peeking via `mix-blend-multiply` + edge-fade mask + brand glow) that flips to a success-toned `Guided reset completed` state once the guide is finished; the gate message reads "Run the guide above and tap Done to confirm." The guide-seen gate (`resetGuideSeen`), the `devicePrep` state shape, the Android inline accordion, and the credentials/unlink path are all unchanged. Tweaks panel from the prototype (accent/density/tone/motion) dropped — ships the fixed Revibe defaults. See `docs/output/returns/change_of_mind.md` §2.4. (Supersedes the earlier checklist-style `ResetGuideSheet` bullets below.)
- Optional battery-health self-check added to Step 2 of the **issue and warranty** raise-a-claim flows (both reuse `Step2IssueDetails`), surfaced only on the `battery` ("Battery draining") sub-type. New `BatteryHealthCheck` card: a capacity-% input (Settings → Battery → Battery Health) + a "battery/part isn't original" self-report toggle, both held in `state.batteryCheck` (`SET_BATTERY_CHECK`) and **never gating `canAdvance`** — the customer can skip it and submit proof + description as before. The card derives the device's guaranteed battery floor from its condition grade (last segment of `product.variant`: excellent 95, very good 90, good 85, fair→85) and time-since-delivery from `deliveredOn`, then shows a live, generic verdict (no flow-switching nudge) per §7.2 Battery Standards via new pure helpers in `src/lib/returns.js` (`BATTERY_BASELINE_BY_GRADE`, `conditionGradeOf`, `batteryBaselineFor`, `daysSinceDelivery`, `assessBattery`): non-original part → full refund; >3% degradation within 10 days → full refund; >10% within 6 months or >20% within 12 months → free battery replacement; otherwise normal wear (still submittable). The time window is part of the calculation (each tier needs both the degradation threshold and its window, checked against `deliveredOn`) and is now surfaced in the UI — the card shows how many days ago the device was delivered, each remedy names its window (10 days / 6 months / 12 months), and a collapsible "What counts as a battery defect?" disclosure (`BatteryThresholds`) lists all three §7.2 thresholds + the normal-wear / non-original rules so a customer can read the policy without entering anything. Each verdict carries an "estimate only — QC confirms" caveat. A filled-in result is carried onto the claim as `claim.batteryAssessment` for the issue + warranty shapes (`buildClaim`) — data only, no tracking-card surface yet. `Step2IssueDetails` now receives `order`. See `docs/output/returns/issue.md` §2.2 + `warranties_compensations.md` §2.4.
- New `claim_compensation` journey added to `src/data/journey.js` (7th entry in `JOURNEYS`). The simplest claim graph — no pickup / transit / device-prep / packing / reset legs, since the customer keeps the device. Reuses the pre-claim chain through `delivered`, then forks at submit on sub-type (`claim_submitted_shipping_refund` / `claim_submitted_charger` — no refund-method fork, the amount is deferred either way; both seed `amountPending: true` with no `expectedRefund`). Three downstream forks: review outcome (`claim_under_review` → `claim_refund_issued`, which reveals the amount by dropping `amountPending` and setting a sub-type-aware `expectedRefund`, → `claim_refund_credited`; **or** `claim_invalid_confirmed`, which sets `claim.invalidClaim` with ops copy only — no `returnShipping`/`returnShipment` — routing to `InvalidClaimCard` → `CompensationClosedCard` as a terminal with no pay/decline sub-branch); and an unclear-evidence loop (`claim_evidence_unclear` sets `claim.docsRejection` + `subStatusId: 'awaiting_documents'` + `actionRequired`, reusing the issue flow's `DocsRejectedCard` takeover, then `claim_evidence_resubmitted` clears it, sets `proofResubmission`, and re-merges into review). Ops messages are sub-type-aware (read from `o.claim.compensationSubtype` inside `apply`). `handleSubmitClaim` in `App.jsx` gains a compensation case mapping the real ClaimFlow submit to `claim_submitted_${compensationSubtype}`. No component changes — `DocsRejectedCard`, `InvalidClaimCard`, `CompensationClosedCard`, and `ClaimCard` are all flow-agnostic and read the projected claim shape. `docs/output/journey_backend_spec.md` + `warranties_compensations.md` §3.8 updated.
- Compensation claim flow wired end-to-end (was a Step 1 stub). Step 1's `Request compensation` now selects `claimType: 'compensation'` instead of showing the not-in-build note; the customer keeps the device, so the flow is refund-only and **skips Steps 3 (device prep), 4 (packing), 5 (pickup)** — 5 visible steps (1 → 2 → 6 → 7 → 8), driven by `visibleStepCount('compensation') = 5` and `NEXT`/`BACK`/`visibleStepIndex` step-overs in `flowReducer.js`. New `src/components/ClaimFlow/compensationSubtypes.js` (`shipping_refund` / `charger`, mirrors `issueSubtypes.js`) + new `Step2Compensation.jsx` (sub-type picker + description + evidence upload; reuses `state.issueDetails` for description/attachment, adds `state.compensationSubtype` via `SET_COMPENSATION_SUBTYPE`). Refund amount is **always "confirmed by support after review"** — no figure shown at submission; `Step5RefundMethod` gains a `CompensationDestination` branch (Wallet vs original payment, no amount/bonus/fee), `Step6Review` shows a "What happened" + TBD-refund section (no device/packing/pickup sections, no ack gate), `Step7Confirmation` swaps to "Your compensation request is in" / "Track this compensation". `ClaimFlow.buildClaim` emits a compensation-shaped claim (`type: 'compensation'`, `compensationSubtype`, `amountPending: true`, no `scheduledPickup`/`devicePrep`/`pickupDetails`/`expectedRefund`); `handlePrimary` skips the factory-reset/packing ack gate for compensation. Tracking reuses `ClaimCard`: new `COMPENSATION_CLAIM_STATUSES` (Submitted → Under review → Refund issued → Refund credited — no Pickup leg) + `claimStatusesFor(claim)` in `lib/claims.js`; `ClaimCard` picks the type's status list for the dot timeline and renders "To be confirmed" when `claim.expectedRefund` is absent. `ClaimDetailsSheet` gains a compensation branch (claim sub-type + description, no pickup/device-prep rows, TBD refund). Invalid verdict has no return-shipping gate — `InvalidClaimCard` short-circuits to a new `CompensationClosedCard` ("Claim closed — no refund", muted tone, "Discuss with support" CTA) when `claim.type === 'compensation'`. Three hand-seeded mocks in `orders.js`: 89630 (under review, shipping refund), 89605 (refunded, charger), 89572 (closed-invalid, charger). See `docs/output/warranties_compensations.md` §3.
- Step 3 (Device prep) iOS reset path replaced its inline accordion with a launch button that opens a new full-screen guided sub-sheet, `src/components/ClaimFlow/ResetGuideSheet.jsx` (renders via `createPortal` to `document.body`, slide-up, `max-h-[94vh]`). A `First — how will you do this?` chooser routes to one of two named, tickable checklists — **Reset from the device** (brand, "most people": back up → sign out of iCloud → erase → confirm Hello screen, with inline danger flags that escalate to the remote route) and **Remove it remotely** (warn: icloud.com/find + appleid.apple.com for broken-biometrics / dead / still-locked devices). An optional **Final touches** checklist (SIM, Apple Watch, iMessage dereg, IMEI photo, order number) sits at the end of each route. Checklist ticks persist in `devicePrep.resetGuideChecks`; a footer `Done` button closes the sheet. Android keeps its existing inline chapter-style accordion. Reset-guide `Note`/`Flag`/`Escalate`/`Ext` are private helpers; `renderBold` parses `**bold**` UI labels. (No "Still stuck?" contact box.)
- Fixed `ResetGuideSheet` not scrolling: the scroll region was `flex-1 min-h-0 overflow-y-auto` **plus** `flex flex-col`, and the fixed-height flex column compressed its tall route section to fit (the section's own `overflow-hidden` then clipped the steps), so content never overflowed the scroll container. Switched the scroll region to a plain block (`space-y-5`) so children keep their natural height and the container actually scrolls. (Same latent pattern exists in `ClaimDetailsSheet` but is never tripped — its content is short.)
- `ResetGuideSheet` copy trim: removed all "forgot Apple ID password" references — dropped the clause from both route-chooser option titles ("I can unlock this device" / "Face ID/Touch ID is broken, or the device won't turn on") and deleted the forgotten-password info `Note` from the remote route.
- Step 3 (Device prep) iOS reset confirm checkbox gained a guide-seen gate: it's locked until the customer opens the guide and taps `Done` (`devicePrep.resetGuideSeen`). Soft-validated like the Step 7 acks — `canAdvance` keeps `Continue` clickable on iOS while the guide is unseen; `ClaimFlow.handlePrimary` intercepts a premature click (new `devicePrepAttempted` flag), paints the guide launch button red (`border-danger`) and pops the danger-toned message *"Open the guide above and tap Done before you can confirm."* (AlertTriangle + `animate-slideDown`) instead of advancing. Clears once the guide is completed; Android (inline accordion) has no such gate. `flowReducer` `devicePrep` shape adds `resetGuideChecks` + `resetGuideSeen`.
- Step 3 (Device prep) factory-reset guide and unlink guide both restructured from flat 3–4-bullet lists into a chapter-style multi-step format. Each chapter has a numbered Step circle, a title, an optional emphasised lead + body, an optional nested sub-action list (authored with `**bold**` markers so UI labels like **Settings** / **Sign Out** / **Erase This Device** render bold), and an optional success-tinted verification tip (BadgeCheck icon, `bg-success-bg/40` border). The critical chapter in each guide carries the *"This is the single most important step in this entire guide"* lead — iOS frames it as Activation-Lock prevention, Android frames it as Factory-Reset-Protection prevention. Reset guide gains a backup-first chapter and a "leave the device on the Hello/welcome screen" closing chapter (so the customer doesn't re-link the device after the wipe). Unlink guide gains a verification tip on the critical erase-and-remove chapter (device should disappear from the device list). New `GuideStep` + `renderBold` helpers shared between `ResetPanel` and `UnlinkPanel` in `src/components/ClaimFlow/Step3DevicePrep.jsx`.

> Older history: see [CHANGELOG-archive.md](CHANGELOG-archive.md)
